% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{plot.bs_surprise_sf}
\alias{plot.bs_surprise_sf}
\title{Plot Surprise Map (sf)}
\usage{
\method{plot}{bs_surprise_sf}(
  x,
  y = NULL,
  which = c("surprise", "signed_surprise"),
  pal = NULL,
  breaks = NULL,
  nbreaks = 9,
  main = NULL,
  border = "grey50",
  lwd = 0.2,
  key.pos = 4,
  ...
)
}
\arguments{
\item{x}{A \code{bs_surprise_sf} object}

\item{y}{Unused (for S3 method compatibility)}

\item{which}{Which variable to plot: "surprise" or "signed_surprise"}

\item{pal}{Color palette. If NULL, uses default diverging palette for
signed surprise and sequential for unsigned.}

\item{breaks}{Breaks for color scale. If NULL, uses pretty breaks.}

\item{nbreaks}{Number of breaks if \code{breaks} is NULL}

\item{main}{Plot title. If NULL, auto-generated.}

\item{border}{Border color for polygons}

\item{lwd}{Line width for borders}

\item{key.pos}{Position of legend (1=below, 2=left, 3=above, 4=right, NULL=none)}

\item{...}{Additional arguments passed to \code{\link[sf:plot]{sf::plot()}}}
}
\value{
Invisibly returns the input object
}
\description{
Plots a surprise map using sf's plot method. For ggplot2 integration,
see \code{\link[=geom_surprise]{geom_surprise()}} and \code{\link[=scale_fill_surprise]{scale_fill_surprise()}}.
}
\examples{
library(sf)
nc <- st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
result <- surprise(nc, observed = SID74, expected = BIR74)

# Default plot
plot(result)

# Plot signed surprise
plot(result, which = "signed_surprise")

# Custom palette
plot(result, pal = heat.colors(9))
}
