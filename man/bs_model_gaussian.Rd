% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-gaussian.R
\name{bs_model_gaussian}
\alias{bs_model_gaussian}
\title{Create a Gaussian Model}
\usage{
bs_model_gaussian(mu = NULL, sigma = NULL, fit_from_data = TRUE, name = NULL)
}
\arguments{
\item{mu}{Mean of the Gaussian. If NULL, estimated from data.}

\item{sigma}{Standard deviation. If NULL, estimated from data.}

\item{fit_from_data}{Logical; estimate parameters from data?}

\item{name}{Optional name for the model}
}
\value{
A \code{bs_model_gaussian} object
}
\description{
Creates a model based on a Gaussian (normal) distribution. This parametric
model is useful for detecting outliers and identifying multiple modes in
data.
}
\details{
The Gaussian model assumes data is drawn from a normal distribution.
Points far from the mean (in terms of standard deviations) will have
low likelihood and thus create high surprise when this model has
probability mass.

This model is particularly useful for:
\itemize{
\item Detecting spatial outliers
\item Identifying multi-modal distributions
\item Combating renormalization bias (outliers get suppressed in dynamic
visualizations)
}
}
\examples{
# Gaussian model with parameters fit from data
model <- bs_model_gaussian()

# Gaussian model with fixed parameters
model <- bs_model_gaussian(mu = 100, sigma = 20)

# Use in model space with other models
population <- c(10000, 50000, 100000, 25000)
space <- model_space(
  bs_model_uniform(),
  bs_model_baserate(population),
  bs_model_gaussian()
)
}
