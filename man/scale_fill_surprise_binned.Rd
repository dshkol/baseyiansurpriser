% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2-scale.R
\name{scale_fill_surprise_binned}
\alias{scale_fill_surprise_binned}
\alias{scale_colour_surprise_binned}
\alias{scale_color_surprise_binned}
\title{Binned Surprise Scale}
\usage{
scale_fill_surprise_binned(
  n.breaks = 5,
  palette = "YlOrRd",
  direction = 1,
  name = "Surprise",
  ...
)

scale_colour_surprise_binned(
  n.breaks = 5,
  palette = "YlOrRd",
  direction = 1,
  name = "Surprise",
  ...
)

scale_color_surprise_binned(
  n.breaks = 5,
  palette = "YlOrRd",
  direction = 1,
  name = "Surprise",
  ...
)
}
\arguments{
\item{n.breaks}{Number of breaks/bins}

\item{palette}{ColorBrewer palette name. For unsigned surprise,
sequential palettes like "YlOrRd", "Oranges", "Reds" work well.}

\item{direction}{Direction of palette (1 = normal, -1 = reversed)}

\item{name}{Legend title}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_brewer]{ggplot2::scale_fill_fermenter()}}}
}
\value{
A ggplot2 scale object
}
\description{
Discrete binned color scale for surprise values. Useful for creating
choropleth maps with clearly distinguishable categories.
}
\examples{
library(ggplot2)
library(sf)

nc <- st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)

# Binned surprise scale - geometry must be mapped explicitly
ggplot(nc) +
  geom_surprise(aes(geometry = geometry, observed = SID74, expected = BIR74)) +
  scale_fill_surprise_binned(n.breaks = 5)
}
