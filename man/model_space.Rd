% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-space.R
\name{model_space}
\alias{model_space}
\title{Create a Model Space}
\usage{
model_space(..., prior = NULL, names = NULL)
}
\arguments{
\item{...}{\code{bs_model} objects or a list of models}

\item{prior}{Numeric vector of prior probabilities (must sum to 1).
If NULL (default), uses uniform prior.}

\item{names}{Optional character vector of names for models}
}
\value{
A \code{bs_model_space} object
}
\description{
Combines multiple models into a model space with prior probabilities.
The model space represents the set of hypotheses about how data is generated.
}
\examples{
# Create model space with uniform prior
space <- model_space(
  bs_model_uniform(),
  bs_model_gaussian()
)

# Create with custom prior
space <- model_space(
  bs_model_uniform(),
  bs_model_baserate(c(0.2, 0.3, 0.5)),
  prior = c(0.3, 0.7)
)

# Create from list
models <- list(bs_model_uniform(), bs_model_gaussian())
space <- model_space(models)
}
