% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{surprise_temporal}
\alias{surprise_temporal}
\title{Compute Temporal Surprise}
\usage{
surprise_temporal(
  data,
  time_col,
  observed,
  expected = NULL,
  region_col = NULL,
  models = c("uniform", "baserate", "funnel"),
  update_prior = TRUE,
  window_size = NULL,
  signed = TRUE,
  ...
)
}
\arguments{
\item{data}{Data frame with time-indexed observations}

\item{time_col}{Column name for time variable (unquoted or string)}

\item{observed}{Column name for observed values}

\item{expected}{Column name for expected values (optional)}

\item{region_col}{Column name for region/spatial identifier (optional).
If provided, computes per-region surprise at each time point.}

\item{models}{Model specification (see \code{\link[=surprise]{surprise()}})}

\item{update_prior}{Logical; should prior be updated after each time step?}

\item{window_size}{For rolling window analysis: number of time periods to include.
If NULL, uses all prior data.}

\item{signed}{Compute signed surprise?}

\item{...}{Additional arguments passed to \code{\link[=compute_surprise]{compute_surprise()}}}
}
\value{
A \code{bs_surprise_temporal} object containing:
\itemize{
\item \code{surprise_by_time}: List of surprise results for each time period
\item \code{time_values}: Vector of time values
\item \code{cumulative_surprise}: Matrix of cumulative surprise
\item \code{model_space}: The model space used
}
}
\description{
Computes surprise over time, allowing beliefs to update as new data arrives.
This is useful for detecting changes in patterns over time and for
streaming data applications.
}
\examples{
# Create temporal data
df <- data.frame(
  year = rep(2010:2020, each = 5),
  region = rep(letters[1:5], 11),
  events = rpois(55, lambda = 50),
  population = rep(c(10000, 50000, 100000, 25000, 15000), 11)
)

# Compute temporal surprise
result <- surprise_temporal(df,
  time_col = year,
  observed = events,
  expected = population,
  region_col = region
)

# View results
print(result)
}
